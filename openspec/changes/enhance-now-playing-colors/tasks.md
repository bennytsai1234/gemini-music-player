# Tasks: Enhance Now Playing Colors

- [x] Verify `MiniPlayer` Material 3 usage (Double check imports/tokens) <!-- id: verify-mini-player -->
- [x] Update `NowPlayingScreen` to use `uiState.gradientColors` for background <!-- id: update-now-playing-bg -->
- [x] Ensure text/icons in `NowPlayingScreen` handle the dynamic background contrast (use `uiState.onBackgroundColor` or automatic M3 content colors) <!-- id: ensure-contrast -->
